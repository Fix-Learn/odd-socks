{
  "name": "Fix & Learn – bug-01-inventory-checkout",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "terminal.integrated.cwd": "/workspaces/${localWorkspaceFolderBasename}/bug-01-inventory-checkout"
      }
    }
  },

  "postCreateCommand": "python -m pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",

  "postStartCommand": "if [ -d bug-01-inventory-checkout ]; then echo '✅ Found folder: bug-01-inventory-checkout' && ls -la bug-01-inventory-checkout; else echo '❌ Folder bug-01-inventory-checkout NOT found. Listing repo root:' && ls -la; fi"
}
